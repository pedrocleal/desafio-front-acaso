import styled from "styled-components";

export const StyledInput = styled.input`
  width: 100%;
  padding: 16px 22px;
  outline: none;
  border: none;
  background: #1E1F2F;
  border-radius: 6px;
  color: #fff;
  font-size: 16px;

  &::placeholder {
    color: #fff;
    opacity: 0.5;
  }
`
